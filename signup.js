function signup() {
  var username = document.getElementById("signup-username").value;
  var password = document.getElementById("signup-password").value;
  
  // Send a request to the server to save the signup information
  // You can use AJAX or fetch() API to send the request to your server
  // and handle the response accordingly.
  
  // Example code to simulate saving the signup information
  alert("Signup successful! Please login with your new account.");
}